# 2차 검토 (2026-07-29)

검토 대상: `review_response.md`, `contamination_literature.csv`, `experiment_design.csv`,
`experiment_models.csv`, `fig_power_corrected.png`, 그리고 원본 두 문서.

---

## 0. 가장 중요한 문제 — 원본 문서 두 개가 수정되지 않았습니다

`review_response.md`는 A1·A2·B3·B4·B5·C·D를 "(수정 완료)"라고 표시했지만,
실제로 바뀐 것은 **CSV 3개와 그림 1개뿐**입니다. 서술 문서 두 개는 손대지 않았습니다.

파일 수정 시각:

| 파일 | 수정 시각 | 상태 |
|---|---|---|
| `contamination_literature_review.md` | 20:27 | **미수정** (1차 리뷰 이전) |
| `topic3_experiment_plan.md` | 21:51 | **미수정** (1차 리뷰 이전) |
| `contamination_literature.csv` | 22:19 | 수정됨 |
| `experiment_design.csv` | 22:19 | 수정됨 |
| `experiment_models.csv` | 22:18 | 수정됨 |
| `fig_power_corrected.png` | 22:19 | 신규 |

원본에 그대로 남아 있는 오류(행 번호):

**`contamination_literature_review.md`**

- 99행: `양자화로 인한 정확도 차이는 종종 1–4%p 수준인데(arXiv:2505.20276)` ← A1이 인정한 오인용 그대로
- 98행: `현재 태스크당 50문제입니다` ← C1
- 44·46행: `당신 Task B` ← C4
- 38행: 2502.14425를 `가장 포괄적인 최신 서베이`로 서술 ← D (CSV만 고쳐짐)
- 20–23행: 단방향 삼단논법 ← C3
- 29–31행: 2501.18771 "당신도 같은 구조를 쓸 수 있습니다" ← B5

**`topic3_experiment_plan.md`**

- 42행: 소제목 `arXiv:2603.03203 — "암기가 없으면 오염을 탐지할 수 없다"` ← A2의 핵심 지적 그대로
- 98행: `| Gemma-4-31B-it | 31B | bf16 | Gemini 계열 | 주력 |` ← B4에서 "제외"로 바꾸겠다고 한 항목이 여전히 **주력**
- 131행: `Welch t-검정` ← A3
- 143행: `10pp이면 약 560개, 5pp이면 약 2,200개` ← A3
- 145행: `현재 설계(태스크당 50문제)의 검정력은 0.07` ← C1
- 181행: `` `fig_power_analysis.png` — 검정력 곡선 `` ← **파일이 삭제되어 깨진 참조**
- 55행: `당신의 Task B 설계` ← C4

`review_response.md`는 대응 계획서로는 훌륭하지만, 계획서와 원본을 둘 다 배포하면
**서로 모순되는 두 문서**가 됩니다. 원본에 반영하거나, 원본을 폐기하고 대응 문서를 정본으로
승격하거나 둘 중 하나를 하십시오.

---

## 1. 이번 수정에서 새로 생긴 오류 2개

### 1-1. `contamination_literature.csv` — arXiv ID에서 자릿수 하나가 사라졌습니다

```
D. 탐지의 한계 (반드시 알 것),2311.0485,Rethinking Benchmark and Contamination with Rephrased Samples,...
```

`2311.0485` → **`2311.04850`**. 같은 행의 URL은 정상이라 눈에 안 띄지만,
BibTeX를 ID 열에서 생성하면 깨집니다. 1차 검토 때는 정상이었으므로 편집 중 유실된 것입니다.

### 1-2. 깨진 그림 참조

`fig_power_analysis.png`가 사라지고 `fig_power_corrected.png`가 생겼는데,
`topic3_experiment_plan.md` 181행은 여전히 옛 파일명을 가리킵니다.

---

## 2. `review_response.md`의 B1 표가 자기 절차대로 재현되지 않습니다

문서의 전제: "**로그오즈 스케일에서 양자화 효과가 완전히 동일할 때**".
그 전제대로 p=0.85와 p=0.35에 동일한 로그오즈 감소 d를 적용해 재계산했습니다.

| d | 문서의 가짜 교차효과 | 재계산 | 차이 |
|---|---|---|---|
| 0.25 | −1.4pp | **−2.0pp** | 어긋남 |
| 0.50 | −2.2pp | **−2.8pp** | 어긋남 |
| 0.75 | −2.5pp | −2.5pp | 일치 |
| 1.00 | −2.2pp | **−1.0pp** | 어긋남 |

문서의 p값을 역산하면 d=0.25 행에서 실제 적용된 감소는 HumanEval 쪽 0.192,
LCB 쪽 0.181입니다. **두 조건에 서로 다른 값이 적용되었습니다** — 이는
"완전히 동일한 로그오즈 효과"라는 전제 자체를 위반합니다.

이것이 같은 절에 나오는 `로그오즈 스케일 교차효과는 +0.023 (사실상 0)`도 설명합니다.
효과가 진짜로 동일하다면 로그오즈 교차효과는 **정확히 0**이어야 합니다.
+0.023이 나온 것은 시뮬레이션 버그의 증거이지 검증 성공이 아닙니다.

**다행히 결론은 살아남습니다.** 재계산해도 가짜 교차효과는 −1 ~ −3pp이고 부호도 음수라,
"5pp 효과를 찾는데 절반 크기의 편향이 있다"는 논지와 "부호가 §3.1의 음수 가설과 겹쳐
거짓 증거가 된다"는 지적 모두 유효합니다. 표만 다시 생성하면 됩니다.

### 덧붙여 — 로그오즈는 "교란 제거"가 아니라 "가정"입니다

`B1` 절은 로그오즈가 `교란을 제거합니다`라고 단정합니다. 정확히는,
**양자화 효과가 오즈비 스케일에서 일정하다는 모형을 가정**하는 것입니다.
그 가정이 틀리면(예: 양자화가 어려운 문제에만 유독 해로움) 로그오즈에서도 편향이 남습니다.
문서가 이미 언급한 난이도 계층화 분석을 "부수 분석"이 아니라 **가정 검증 절차**로 격상하십시오.

---

## 3. A3의 페어링 방어와 새 그림이 서로 다른 이야기를 합니다

`review_response.md` A3은 "560은 **페어링 설계**에서 맞는 값"이라며 페어링 검정력표
(n=560에서 0.82)를 제시해 원래 숫자를 방어합니다.

그런데 `fig_power_corrected.png` 패널 (a)는 부제가 `기저 정확도 0.5, 가장 보수적`이고,
곡선을 읽으면 10pp가 0.8에 도달하는 지점이 **약 800**, 5pp는 **약 3.2k**, 20pp는 **약 200**입니다.
제가 독립 계산한 비페어링 값(785 / 3,140 / 196)과 정확히 일치합니다.
즉 **그림은 페어링을 버리고 비페어링 수치를 채택했습니다.**

문서 본문은 560을 방어하고 그림은 785를 그리고 있습니다. 하나로 정하십시오.
그림 쪽(보수적 비페어링)을 정본으로 삼는 편을 권합니다 — 페어링 이득은
두 정밀도 간 문항 수준 상관에 의존하는데, 그 상관값이 문서 어디에도 명시되어 있지 않아
560은 현재 상태로는 **재현 불가능한 숫자**입니다. 페어링을 유지하려면 가정한 상관을 명시하십시오.

---

## 4. "수정 완료"로 표시됐지만 CSV에도 반영되지 않은 항목

| 항목 | 선언 | 실제 |
|---|---|---|
| B5 톤 하향 | "수정 완료" | `contamination_literature.csv`의 2501.18771 관련성 열이 여전히 `당신이 모방할 실험 설계의 정석` |
| C4 Task B 용어 | "용어를 바꿉니다" | CSV 분류 열 4개 행이 여전히 `B. 시간 분할 = 당신 Task B의 근거` |
| D 2605.15138 설명 보강 | "수정 완료" | 이 논문은 CSV에 아예 없고 플랜 문서도 미수정 — **어디에도 반영 안 됨** |
| A1 인용 교체 | "완료" | 새 3편(2503.07103 / 2507.09665 / 2506.22776)이 CSV에 추가되지 않음 |

---

## 5. 셀 수와 오염 조건 개수가 맞지 않습니다

`review_response.md` C2: `4 오염조건 × 4 양자화 × 3 모델 = 48셀`.

그런데 `experiment_design.csv`의 오염 축은 LCB pre / LCB post / HumanEval / MBPP+ 4행이면서,
HumanEval 행 비고에 `HumanEval과 합쳐 542`, MBPP+ 행 설명에 `오염 조건 n 보강`이라고 적혀 있습니다.
둘을 **하나의 풀링된 보조 조건**으로 쓴다는 뜻이라면 오염 조건은 3개이고 셀은 **36개**입니다.
B2 절의 상한 표도 542를 단일 arm으로 계산했습니다(그 계산은 정확합니다).

풀링 여부를 명시하십시오. HumanEval과 MBPP+는 난이도 분포가 달라서 풀링하면
B1이 지적한 기저 정확도 교란이 조건 **내부**로 들어옵니다.

## 5-1. 새 그림 패널 (b)가 이미 폐기한 설계를 설명합니다

패널 (b) 제목은 `실제 병목: 오염 조건의 문제 수`이고 164 → 15.5pp를 강조합니다.
그런데 채택된 대응(B2-1)은 **LCB pre-cutoff를 주 오염 조건으로 승격(n≥1000)**하는 것이므로,
새 설계에서는 164 상한이 더 이상 구속하지 않습니다.
패널 (b)는 "왜 설계를 바꿨는가"를 보여주는 그림이지 "현재 설계의 병목"이 아닙니다.
제목을 `HumanEval을 주 조건으로 쓸 수 없는 이유`로 바꾸는 게 정확합니다.

---

## 6. 새 인용 3편 — 전부 검증 통과, 다만 단서 하나

원문 조회 결과 세 편 모두 실재하고 인용 내용이 정확합니다.

- **2503.07103** "Evaluating the Impact of Post-Training Quantization on LLMs for Code Generation"
  (최대 34B, 2bit까지). 초록 원문: *"the new frontier for LLM quantization is 4-bit precision,
  resulting in an average memory footprint reduction of 70% ... without observing any significant
  decrease in performance."* → 인용 정확.
- **2507.09665** "Is Quantization a Deal-breaker? Empirical Insights from Large Code Models"
  (AWQ, CodeLlama·DeepSeekCoder). 기능적 정확성 + 유지보수성·구조적 단순성 보존 → 인용 정확.
- **2506.22776** "Smaller = Weaker? Benchmarking Robustness of Quantized LLMs in Code Generation"
  (350M–33B, LLaMA/DeepSeek/CodeGen/StarCoder). **51.59% vs 42.86%** → 인용 정확.

**단서**: 세 편 모두 **AWQ 또는 캘리브레이션 기반 GPTQ**를 **코드 특화 모델**에 적용한 결과입니다.
당신 설계의 주 4-bit 조건은 **캘리브레이션 없는 BNB-nf4**입니다.
그리고 2505.20276은 바로 그 BNB-nf4에서 Llama-3.1-70B가 32% 떨어졌다고 보고합니다.

따라서 `리뷰가 놓친 것 하나` 절의 "코드 도메인에서는 저하가 거의 없으므로 교차효과가
5pp보다 작을 것" 결론은 **AWQ/GPTQ arm에는 타당하지만 bnb arm에는 과도하게 비관적**입니다.
이건 오히려 좋은 소식입니다 — B3에서 추가하기로 한 기법 축이 단순한 일반화 보강이 아니라
**효과 크기가 가장 클 것으로 예상되는 조건을 확보하는 수단**이 됩니다.
파일럿은 bnb-nf4 arm에서 먼저 돌리십시오.

---

## 7. 정확했던 것 (수정 불필요)

- **B2 상한 표**: 완전 일치. 재계산 결과 164→15.5pp, 300→11.4pp, 542→8.5pp, 1,000→6.3pp,
  2,000→4.4pp(문서 4.5pp, 반올림 차이). 계산 방식도 옳습니다.
- **새 그림 패널 (a)**: 비페어링 p=0.5 기준 정확 (20pp≈196, 10pp≈785, 5pp≈3,140).
- **`contamination_literature.csv` 서지**: published 빈칸 6개 전부 올바른 값으로 채워짐.
  2404.00699·2502.17521 정식 제목 반영, `[sic]` 처리도 적절.
- **`experiment_models.csv` / `experiment_design.csv`**: B3(GPTQ/AWQ 추가, double-quant 제거),
  B4(Gemma 교란 명시), B2(LCB pre 승격, MBPP+ 추가), C5(모델 내 상대변화), C6(아키텍처 열 삭제)
  모두 정확히 반영됨. 이 세 파일은 잘 정리됐습니다.
- **A2의 재구성**: 전제를 좁히면서 "양자화가 탐지 기법 간 순위를 뒤집는가"로 연구질문을
  구체화한 것은 원래 삼단논법보다 낫습니다. 검증 가능하고 결과가 어느 쪽이든 논문이 됩니다.
- **A1의 확장 논증**: "59%는 롱컨텍스트 수치이므로 코드 도메인에 옮기면 안 되고,
  그 결과 B2가 더 나빠진다"는 지적은 제 1차 리뷰의 프레이밍을 바르게 고친 것입니다.

---

## 다음에 할 일 (순서대로)

1. **원본 두 문서에 실제로 반영하십시오.** 이게 안 되면 나머지는 의미가 없습니다.
   최소한 `topic3_experiment_plan.md`의 42·98·131·143·145·181행, 
   `contamination_literature_review.md`의 99행.
2. `contamination_literature.csv`의 `2311.0485` → `2311.04850`.
3. B1 표를 동일 로그오즈 감소로 재생성 (또는 시뮬레이션 버그 수정).
4. 560 vs 785 중 하나로 확정. 페어링을 쓸 거면 가정한 문항 간 상관을 명시.
5. 오염 조건 개수(3 or 4)와 셀 수(36 or 48) 확정.
6. 새 인용 3편을 `contamination_literature.csv`에 G 분류(코드 도메인 양자화)로 추가.
7. 그림 파일명 참조 정정 + 패널 (b) 제목 수정.
