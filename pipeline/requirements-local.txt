# Mock-only profile: no GPU library required.
# Enough to run the dry-run harness (scripts/run_dry_run.py) and the
# GPU-free pytest suite (tests/) end to end.
numpy
pandas
pyarrow
pytest
# CPU-only tokenizer for models/mock.py (GPT2TokenizerFast) — transformers
# itself doesn't require torch to import or to do pure tokenization; the
# GPU-requiring paths (model loading, generation) live in models/loader.py,
# not here. Was missing from this file even though mock.py already imported
# it — bug found while wiring up the dry-run harness.
transformers
# Pinned: item counts (164 HumanEval+, 378 MBPP+) verified against this
# exact version during pipeline construction (constants.HUMANEVAL_N_ITEMS /
# MBPPPLUS_N_ITEMS) — a later release has historically shipped different
# MBPP+ subset sizes, so this is not a "just take latest" pin.
evalplus==0.3.1
datasets
huggingface_hub
